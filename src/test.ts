/*import { Vec3 } from "wgpu-matrix";

class Chunk {
  constructor(
  public blocks?: Uint16Array = new Uint16Array(3000),
    public offset?: number,
  ) {}
}



class ChunkManager {
  static fill(chunk: Chunk, block: number): Chunk {
    chunk.blocks.map((_) => block);
    chunk.offset = 3;
    return chunk;
  }
  }*/

// data: objects
// managers: for single objects
// systems: intertwine objects and logic
